# CardioAI Backend Dependencies
# Using >= constraints so pip picks pre-built wheels for the runtime Python version
# Avoids source compilation (which times out on Render free tier)

flask>=3.0,<4.0
flask-cors>=4.0,<5.0
wfdb>=4.3.1,<5.0
pandas>=2.2.3,<3.0
numpy>=1.26,<3.0
scipy>=1.11,<2.0
gunicorn>=21.0,<24.0
pymupdf>=1.23,<2.0
opencv-python-headless>=4.8,<5.0
requests>=2.31,<3.0
